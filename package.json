{
  "name": "prompt_template",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env-file .env",
    "dev:local": "wrangler dev --env-file .env --local",
    "deploy": "wrangler deploy",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "latest"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
